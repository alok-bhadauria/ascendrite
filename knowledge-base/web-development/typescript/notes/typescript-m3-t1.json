{
  "topic_id": "typescript-m3-t1",
  "title": "Variance",
  "version": "1.0.0",
  "topic_metadata": {
    "difficulty": "Hard",
    "estimated_hours": 6,
    "importance": "High"
  },
  "learning_outcomes": [
    "Describe structural characteristics of Variance.",
    "Derive mathematical and programmatic bounds for Covariance types conversions, Contravariance function parameters constraints, Bivariance traps."
  ],
  "content_sections": [
    {
      "title": "Variance",
      "content": "### Compiler Configurations, Generics & Type Variances\nTypeScript adds static typing checks to JavaScript codebases.\n\n#### Const Assertions & Widening\n*   **Literal Widening:** Variables declared with `let` widen to base types (e.g. `let a = 'GET'` is type `string`).\n*   **Const Assertions (`as const`):** Prevents literal widening and marks all nested properties as `readonly`, locking types to literal values.\n\n#### Conditional Types & infer\nConditional types evaluate structural type relations:\n$$T \\text{ extends } U \\text{ ? } X \\text{ : } Y$$\n*   `infer`: Declares a placeholder variable inside the conditional check to extract sub-types dynamically (e.g. extracting function return types).\n\n#### Parameter Type Variances\n*   **Covariance:** Subtyping direction matches. If `Dog extends Animal`, then `List<Dog>` is assignable to `List<Animal>`.\n*   **Contravariance:** Subtyping direction reverses. Applies to function parameter arguments. A function accepting `Animal` is assignable to one accepting `Dog` because the handler is prepared to process any animal type.\n*   *strictFunctionTypes:* Enforces contravariant parameter validation, preventing runtime bivariant type bypass errors.\n\n### Asymptotic and Spatial Complexity Analysis\nWe summarize the computational characteristics for Variance implementations in the following reference matrix:\n\n| Process/Operation | Worst-Case Time | Average-Case Time | Space Complexity |\n| :--- | :--- | :--- | :--- |\n| Core Logic Execution | $O(N)$ | $O(N)$ | $O(1)$ |\n| Memory Operations | $O(N)$ | $O(1)$ | $O(N)$ |\n\n### Interactive Virtualization Configuration\nTo aid conceptual comprehension, the web client maps this topic to the `type-variance-checker` simulation runtime, drawing state vectors from the following callout schema.",
      "callouts": [
        {
          "type": "Student Trap",
          "title": "Common Trap in Variance",
          "content": "A common mistake when analyzing Variance is ignoring latency bounds and boundary edge cases during concurrent executions."
        },
        {
          "type": "Engineering Note",
          "title": "Visualizer Configuration Model",
          "content": "{\"visualizer_type\": \"type-variance-checker\", \"interactive\": true, \"schema\": {\"base_class\": \"Animal\", \"sub_class\": \"Dog\", \"covariant_assignment\": \"List<Dog> to List<Animal>\", \"contravariant_assignment\": \"(Animal)=>void to (Dog)=>void\"}}"
        }
      ]
    }
  ],
  "example_refs": [
    "typescript-m3-t1-ex1"
  ],
  "diagram_refs": [
    "typescript-m3-t1-dia1"
  ],
  "practice_refs": [
    "typescript-m3-t1-prac1"
  ],
  "quiz_refs": [
    "typescript-m3-t1-quiz"
  ]
}