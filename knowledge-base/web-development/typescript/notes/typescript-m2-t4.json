{
  "topic_id": "typescript-m2-t4",
  "title": "Utility Types",
  "version": "1.0.0",
  "topic_metadata": {
    "difficulty": "Hard",
    "estimated_hours": 6,
    "importance": "High"
  },
  "learning_outcomes": [
    "Describe structural characteristics of Utility Types.",
    "Derive mathematical and programmatic bounds for Partial, Required, Readonly, Pick, Record, Omit type transformations under the hood."
  ],
  "content_sections": [
    {
      "title": "Utility Types",
      "content": "### Compiler Configurations, Generics & Type Variances\nTypeScript adds static typing checks to JavaScript codebases.\n\n#### Const Assertions & Widening\n*   **Literal Widening:** Variables declared with `let` widen to base types (e.g. `let a = 'GET'` is type `string`).\n*   **Const Assertions (`as const`):** Prevents literal widening and marks all nested properties as `readonly`, locking types to literal values.\n\n#### Conditional Types & infer\nConditional types evaluate structural type relations:\n$$T \\text{ extends } U \\text{ ? } X \\text{ : } Y$$\n*   `infer`: Declares a placeholder variable inside the conditional check to extract sub-types dynamically (e.g. extracting function return types).\n\n#### Parameter Type Variances\n*   **Covariance:** Subtyping direction matches. If `Dog extends Animal`, then `List<Dog>` is assignable to `List<Animal>`.\n*   **Contravariance:** Subtyping direction reverses. Applies to function parameter arguments. A function accepting `Animal` is assignable to one accepting `Dog` because the handler is prepared to process any animal type.\n*   *strictFunctionTypes:* Enforces contravariant parameter validation, preventing runtime bivariant type bypass errors.\n\n### Depth Analysis: Utility Types\nIn production configurations, we analyze how Partial, Required, Readonly, Pick, Record, Omit type transformations under the hood affects performance parameters. We evaluate memory layouts and consensus thresholds.",
      "callouts": [
        {
          "type": "Student Trap",
          "title": "Common Trap in Utility Types",
          "content": "A common mistake when analyzing Utility Types is ignoring latency bounds and boundary edge cases during concurrent executions."
        },
        {
          "type": "Engineering Note",
          "title": "Production Scaling for Utility Types",
          "content": "Production servers implement strict resource isolation and cache alignments to prevent performance regressions when running Utility Types loops."
        }
      ]
    }
  ],
  "example_refs": [
    "typescript-m2-t4-ex1"
  ],
  "diagram_refs": [
    "typescript-m2-t4-dia1"
  ],
  "practice_refs": [
    "typescript-m2-t4-prac1"
  ],
  "quiz_refs": [
    "typescript-m2-t4-quiz"
  ]
}