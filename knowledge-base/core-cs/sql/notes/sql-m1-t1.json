{
  "topic_id": "sql-m1-t1",
  "title": "SQL Block Execution Order",
  "version": "1.0.0",
  "topic_metadata": {
    "difficulty": "Hard",
    "estimated_hours": 6,
    "importance": "High"
  },
  "learning_outcomes": [
    "Describe structural characteristics of SQL Block Execution Order.",
    "Derive mathematical and programmatic bounds for The compiler logical parsing order (FROM -> JOIN -> WHERE -> GROUP BY -> HAVING -> SELECT -> ORDER BY), Why aliases created in SELECT are not available in WHERE."
  ],
  "content_sections": [
    {
      "title": "SQL Block Execution Order",
      "content": "### Logical Query Processing & Execution Optims\nSQL is declarative. The database engine parses, optimizes, and compiles SQL queries into physical execution plans.\n\n#### Logical Processing Sequence\nThe order of processing differs from lexical ordering:\n1.  `FROM` + `JOIN` (Load tables and calculate Cartesian product)\n2.  `ON` (Filter join conditions)\n3.  `WHERE` (Filter row subsets)\n4.  `GROUP BY` (Aggregate rows into groups)\n5.  `HAVING` (Filter group subsets)\n6.  `SELECT` (Project output columns)\n7.  `DISTINCT` (Remove duplicate outputs)\n8.  `ORDER BY` (Sort output rows)\n9.  `LIMIT` (Trunk row outputs)\n*Trap:* Referencing select aliases in the `WHERE` clause triggers compilation errors because the projection has not executed yet.\n\n#### Window Functions ROWS vs RANGE\n*   `ROWS`: Defines the boundary frame strictly by offset counts (e.g. `ROWS BETWEEN 1 PRECEDING AND CURRENT ROW`).\n*   `RANGE`: Defines frame boundaries by value differences based on the sort column (e.g. duplicate sort values are processed as a single frame boundary block).\n\n#### Leftmost Index Prefix Rules\nA composite index on `(A, B, C)` can only optimize queries filtering on `A`, `(A, B)`, or `(A, B, C)`. If a query filters on `B` and `C` without filtering on `A`, the index cannot be traversed because sorting is ordered hierarchically by column indices.\n\n### Depth Analysis: SQL Block Execution Order\nIn production configurations, we analyze how The compiler logical parsing order (FROM -> JOIN -> WHERE -> GROUP BY -> HAVING -> SELECT -> ORDER BY), Why aliases created in SELECT are not available in WHERE affects performance parameters. We evaluate memory layouts and consensus thresholds.",
      "callouts": [
        {
          "type": "Student Trap",
          "title": "Common Trap in SQL Block Execution Order",
          "content": "A common mistake when analyzing SQL Block Execution Order is ignoring latency bounds and boundary edge cases during concurrent executions."
        },
        {
          "type": "Engineering Note",
          "title": "Production Scaling for SQL Block Execution Order",
          "content": "Production servers implement strict resource isolation and cache alignments to prevent performance regressions when running SQL Block Execution Order loops."
        }
      ]
    }
  ],
  "example_refs": [
    "sql-m1-t1-ex1"
  ],
  "diagram_refs": [
    "sql-m1-t1-dia1"
  ],
  "practice_refs": [
    "sql-m1-t1-prac1"
  ],
  "quiz_refs": [
    "sql-m1-t1-quiz"
  ]
}